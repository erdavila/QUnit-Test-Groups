- Make tests pass in Chrome/Firefox/IE/Opera/Safari.

- Documentation (in wiki)
	- How to use.
	- Known issue: error on dynamic loading breaks on Firefox if running under file://

- Ensure order of dynamically loaded scripts.

- Review structure of source code.

- Support for simpler objects:
	{"name":name, "file": file}  is the same as  new TestFile(name, file)
	{"name":name, "items": items}  is the same as  new TestGroup(name, items)
	-> Eliminate classes TestFile and TestGroup and turn static all methods (into TestGroups)?
	
	var rootTestGroup = {
		name : "All Tests",
		items : [
			{name : "Test 1", file : "file1.js"},
			{name : "Test 2", file : "file2.js"},
			{name : "Group A", items : [
				{name : "Test A1", file : "test-a1.js"},
				{name : "Test A2", file : "test-a2.js"},
				new TestGroup("Group AX", itemsAX),
			],},
			{name : "Test 3", file : "file3.js"},
			{name : "Group B", items : [
				{name : "Test B1", file : "test-b1.js"},
				new TestFile("Test B2", "test-b2.js"),
				{name : "Test B3", file : "test-b3.js"},
			],},
		],
	};

- Make tests pass with "Check for Globals".

- Checkboxes "Hide passed tests", "Check for Globals" and "No try-catch" in the outline: pass the option to the opened page.

- Outline option to open page in a new tab.

- Refactor tests:
	- Separate in *.js files.
	- Reuse same fixtures in modules TestGroup.searchByName and TestGroups._loadAndRunTestByUrlArg.
	- Use QUnit Test Groups to test QUnit Test Groups!
