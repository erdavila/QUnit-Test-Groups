- Documentation & Licensing.
	Document that dynamic loading breaks on Firefox if running under file://

- Ensure order of dynamically loaded scripts.

- Review structure of source code.

- Support for simpler objects:
	{"name":name, "url": url}  is the same as  new TestFile(name, url)
	{"name":name, "items": items}  is the same as  new TestGroup(name, items)
	
	var rootTestGroup = {
		name : "All Tests",
		items : [
			{name : "Test 1", url : "file1.js"},
			{name : "Test 2", url : "file2.js"},
			{name : "Group A", items : [
				{name : "Test A1", url : "test-a1.js"},
				{name : "Test A2", url : "test-a2.js"},
				new TestGroup("Group AX", itemsAX),
			],},
			{name : "Test 3", url : "file3.js"},
			{name : "Group B", items : [
				{name : "Test B1", url : "test-b1.js"},
				new TestFile("Test B2", "test-b2.js"),
				{name : "Test B3", url : "test-b3.js"},
			],},
		],
	};

- Run tests in Firefox and IE.

- Make tests pass with "Check for Globals".

- Checkboxes "Hide passed tests", "Check for Globals" and "No try-catch" in the outline: pass the option to the opened page.

- Outline option to open page in a new tab.

- Outline could be automatically appended
	- to document.body
	- or to an element specified in an argument.


- Think about:
	* root group should be defined together with all groups (in tests-groups.js).
	* TestGroup.outline() (in index.html) should render according to defined root.
	* TestGroup.runSelectedTests() (in tests.html) should run tests.
